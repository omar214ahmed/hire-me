fastapi==0.110.2
uvicorn[standard]==0.29.0
python-multipart==0.0.9
python-dotenv==1.0.1
pydantic-settings==2.2.1

pdfplumber==0.11.7
pymupdf==1.26.4
python-docx==1.2.0
ftfy 
# Pinned: gliner==0.2.16 only requires transformers>=4.38.2 (no upper bound),
# so an unpinned install grabs the latest transformers (5.x as of this
# writing). transformers 5.x changed tokenizer init internals
# (_set_model_specific_special_tokens) in a way that's incompatible with
# GLiNER's bundled DeBERTa-v2 fast tokenizer and crashes with
# "AttributeError: 'list' object has no attribute 'keys'" during
# AutoTokenizer.from_pretrained(). Keep this on the 4.x line.
transformers==4.44.2
tokenizers==0.19.1
huggingface_hub==0.24.6
gliner==0.2.16
FlagEmbedding==1.3.4
torch
scikit-learn
aiofiles

# ONNX (local INT8 model) inference path
onnxruntime
optimum[onnxruntime]
sentencepiece

asyncpg
pgvector

# JD extraction v2 (pipeline/jd_llm_extractor.py): structured-output LLM
# path used by jd_router for JDs the legacy regex/GLiNER pipeline can't
# reliably parse (emoji headers, inconsistent/missing sections, long
# unstructured or non-English text). Lazily imported - not a hard
# dependency for JD_EXTRACTION_MODE=legacy deployments.
anthropic

celery
redis